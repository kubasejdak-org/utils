.Linux_Sanitizer:
  stage: sanitizers
  image: kubasejdak/gcc:11
  tags:
    - cet_build
  needs: []
  artifacts:
    name: "${CI_JOB_NAME}_${CI_JOB_ID}"
    expire_in: 2 weeks
    paths:
      - ${CI_JOB_NAME}/bin
  script:
    - mkdir ${CI_JOB_NAME}
    - cd ${CI_JOB_NAME}

    # Build application.
    - cmake .. -DPLATFORM=linux -DTOOLCHAIN=gcc-11 -D${Sanitizer}=ON
    - make
    - bin/utils-tests ${TestTags}

Address_Sanitizer_Linux:
  extends: .Linux_Sanitizer
  variables:
    Sanitizer: "USE_ASAN"
    TestTags: "~[SkipAsan]"

Leak_Sanitizer_Linux:
  extends: .Linux_Sanitizer
  variables:
    Sanitizer: "USE_LSAN"

UndefinedBehavior_Sanitizer_Linux:
  extends: .Linux_Sanitizer
  variables:
    Sanitizer: "USE_UBSAN"
